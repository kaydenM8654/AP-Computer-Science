package Q1;
import java.util.*;
public class prog122a {
    public static void main(String[] args) {
        int a = 0;
        int b = 1;
        double c = 0;

        // while condition1 && condition2 [and]
        // while condition1 || condition2 [or]
        while (a < 32 || a = 32) {
            a += 1;
            double sqrt = Math.sqrt(a);
            double sq = a * a;
            if (a == b) {
                b++;
                if (a != 32) {
                    a = 1;
                }
            }
        }
            a += 1;
            double sqrt = Math.sqrt(a);
            double sq = a * a;
            if (a == b) {
                b++;
                if (a != 32) {
                    a = 1;
                }
            }

            System.out.printf("%d/%d\t\t%.5f\n", a, sqrt, sq);

    }
}
